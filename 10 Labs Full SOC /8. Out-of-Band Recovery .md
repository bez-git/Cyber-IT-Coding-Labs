The Scenario: Disablement of the primary management interface (internal LAN), resulting in a total lockout.

The Fix: Executing an emergency access maneuver via the physical DMZ port using a manually assigned Static IP (10.10.10.x).

Key Skills: Disaster Recovery, Static IP Networking, Out-of-Band (OOB) Management.
